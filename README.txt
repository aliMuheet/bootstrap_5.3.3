#1 Open terminal in root of this folder
#2 Run "npm install" (it will install dart-sass)
#3 run "npm run sass" (it will compile all SCSS files into css.) See package.json for input scss and output css folder path
#4 enjoy
